---
export const prerender = false;
export async function GET({ cookies, redirect }) {
  cookies.set('tc-preview', '1', {
    path: '/',
    httpOnly: true,
    sameSite: 'Lax',
    maxAge: 60 * 60 * 12,
  })
  return redirect('/')
}
---
